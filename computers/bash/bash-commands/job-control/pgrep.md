- Search through currently running processes and list the process IDs which match regex

# pgrep specific
- -a -> List the full command line as well as process id
- -f -> Match pattern against command line not just pattern