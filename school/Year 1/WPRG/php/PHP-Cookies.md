- Used to create a mini-profile of the user and save data between webpages
- There exists temporary for the session (browser is open) and permanent ones
- Can only save strings

```php
setcookie('cookiename', ++int)
$_COOKIE['cookiename']
```